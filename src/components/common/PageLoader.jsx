import React from 'react';

const PageLoader = () => (
  <div style={{ 
    display: 'flex', 
    justifyContent: 'center', 
    alignItems: 'center', 
    height: '100vh',
    fontSize: '1.2rem',
    color: '#6366F1',
    fontWeight: 'bold'
  }}>
    Loading...
  </div>
);

export default PageLoader;
